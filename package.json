{
  "devDependencies": {
    "@types/node": "20.8.2",
    "gts": "^5.2.0",
    "live-server": "^1.2.2",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
